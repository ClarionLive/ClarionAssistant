! Round 5 (Defect B): prototype list spliced into DualMapLib.clw's SECOND top-level MAP
! via INCLUDE — part of the NYSCommon shape (INCLUDE + nested MODULE inside the second
! sibling MAP).
DualMapIncProc    PROCEDURE( ), LONG
